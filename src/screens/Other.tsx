import React from 'react';
import { Calligraphy, Container } from '../components';

const OtherScreen = () => {
  return (
    <Container style={{
      justifyContent: 'center',
      alignItems: 'center',
    }}>
      <Calligraphy>Not home</Calligraphy>
    </Container>
  )
};

export default OtherScreen;